data_ingestion:
  artifacts: artifacts/data
  filename: Dataset.csv
  db_name: projects 
  table_name: phishing_url_dataset

data_validation:
  artifacts: artifacts/validation
  validation_status_file: validation_status.json

data_transformation:
  artifacts: artifacts/transformation
  complete_transformed_filename: transformed_data.csv
  useful_features: ['url','dom','is_https','tld','digit_cnt']
  training_data_filename: train_data.csv
  testing_data_filename: test_data.csv
  val_data_filename: val_data.csv
  scaler_filename: datascaler.pkl 
  encoder_filename: dataencoder

model_training:
  artifacts: artifacts/training
  transformed_filepath: artifacts/transformation
  training_data_filename: train_data.csv
  testing_data_filename: test_data.csv
  useful_features: ['url','dom','is_https','tld','digit_cnt']
  num_estimators: 100
  max_depth: 32
  model_filename: model.pkl
  model_version_filename: model_version.json

model_evaluation:
  artifacts: artifacts/evaluation
  val_data_filename: val_data.csv
  model_filename: model.pkl
  model_version_filename: model_version.json
  metrics_filename: metrics.json 
